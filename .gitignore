# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Dr. Gissele · raw media backup folder (309MB of source assets)
# Not used by the site; lives only locally on Dr. Gissele's Mac.
# The site reads from /public/media which IS committed.
/media Photos logos videos/

# Dr. Gissele · personal deliverables that live next to the project
# (PDFs, Word docs, the legacy single-file v3 HTML mock). Not part of
# the deployed site, just stored alongside for convenience.
/*.pdf
/*.docx
/DrGisseleDonovan_Website_v3.html
